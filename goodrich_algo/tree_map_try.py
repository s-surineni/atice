from tree_map import TreeMap
# basic actions of binary search tree

# Insertion
tm = TreeMap()
# Things to notice
# Number of classes
# Is there a separate class for Tree and Node
# Class and instance attributes of tree
# Attributes of node
# How an element is stored in node

# Special cases Adding first element we need to check if tree is empty and add a method to addroot
tm[1] = 'one'                   # This is a special case as this will add root
print(tm[1])
tm[2] = 'two'
print(tm[2])
